<article class="project">
  <section class="name">
    <h2><%= link_to project.name, project_path(project), class: "btn btn-outline-primary btn-lg btn-block" %></h2>
    <h3><%= project.start_date %> | <%= project.end_date %></h3>
  </section>
</article>